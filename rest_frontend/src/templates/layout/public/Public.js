import Container from './Container';
import Header from './Header';

const Protected = () => {
    return (
        <>
            <Header/>
            <Container/>
        </>
    );
}

export default Protected;